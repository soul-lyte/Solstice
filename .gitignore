# Build output
/build/
/.gradle/
/out/

# IDE files
*.iml
*.ipr
*.iws
.idea/
.vscode/
*.classpath
*.project
.settings/

# Gradle wrapper JAR (downloaded automatically)
gradle/wrapper/gradle-wrapper.jar

# OS noise
.DS_Store
Thumbs.db

# Minecraft run dirs (generated by Loom)
run/
logs/

# Temp config (local dev)
config/solstice.json

# Scratch texture-pack sources - already extracted into src/main/resources/resourcepacks/,
# this folder is only a staging area (per its own name) and shouldn't be published.
/NEW - DELETE AFTER USAGE/

# Local research checkouts of dozens of OTHER real mods (sodium, lithium, ModMenu,
# AppleSkin, etc. - 300MB+), kept only for studying their approach, never copied from
# wholesale. Not Solstice's own code and not meant to be published as part of this repo.
/References/

# Local development notes/history - not part of the published source.
/CLAUDE.md
